import { Project } from '../../../components/layouts/Project';
import meta from './meta.json';

export default ({ children }) => <Project title={meta.title} url={meta.url} img={require('./feature.png')}>{children}</Project>

Jump.Club placeholder

